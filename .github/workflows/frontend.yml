name: Frontend CI/CD

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    permissions:          # Required for OIDC
      id-token: write
      contents: read

    steps:
      # Step 1: Checkout code
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Configure AWS credentials via OIDC
      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v3
        with:
          role-to-assume: arn:aws:iam::141701955118:role/GitHub-OIDC-Role
          aws-region: ${{ secrets.AWS_REGION }}
          audience: GitHub-OIDC-Provider

      # Step 3: Build and deploy frontend directly on EC2
      - name: Build and Deploy Frontend on EC2
        run: |
          aws ssm send-command \
            --targets "Key=instanceIds,Values=${{ secrets.EC2_INSTANCE_ID }}" \
            --document-name "AWS-RunShellScript" \
            --comment "Build frontend and deploy to Nginx" \
            --parameters 'commands=[
              "cd /home/ubuntu/frontend",
              "git reset --hard",
              "git pull origin main",
              "npm install",
              "npm run build",
              "rm -rf /var/www/html/*",
              "cp -r build/* /var/www/html/",
              "sudo systemctl reload nginx"
            ]' \
            --region ${{ secrets.AWS_REGION }}